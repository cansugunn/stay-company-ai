package com.student.ai.constants;

public interface SecurityConstants {
    String AUTHORIZATION_HEADER = "Authorization";
    String BEARER_PREFIX = "Bearer ";
    int BEARER_PREFIX_LENGTH = BEARER_PREFIX.length();
}
